# Art pipeline — FEEDBACK

The sprite pass: what art is owed, what prompt writes it, and how much of it there needs to be.

**Quotes are Noodle's, verbatim. Do not summarise this file; add to it.** If an annotation and a
quote disagree, the quote wins. Items were split out of the round-09 file in session 41 and moved
byte for byte — nothing was rewritten on the way.

**Annotate an item the moment it lands**, not in a batch at the end, so a session stopped midway
can be picked up from this file alone. Move finished items to `_archive/FEEDBACK-DONE.md`.

When an item is closed, update the count in `../FEEDBACK.md` — that table is how a session
tells whether the previous one ended before it could write its docs.

Status key: ☐ not started · ◐ in progress · ⏸ waiting on Noodle · ☑ done · ☆ new, unsorted

Where the work is: `../CATCH-UP.md`. What the project is: `../BASICS.md`.

---

### B21. Enemy art scope ☐

> Notes for art passes: Enemies have waaaay too many images. They should have a combat standing pose
> and an attacking pose, that's it for common enemies. More complex ones can have broken sprites and
> other attack poses optionally as needed. Tilting their sprite and making it redder (in-engine!) will
> be enough since vfx will do the rest of the work.

**This cuts the sprite pass, a demo goal, by a large factor.** New baseline:

- **Common enemies: two images.** Standing, attacking. Nothing else.
- **Complex enemies: optional extras** — broken sprites, additional attack poses — only where needed.
- **Hit and hurt states are engine effects, not art.** Tilt the sprite, redden it, in-engine. VFX
  carries the rest.

Two consequences worth acting on: the 13 `artOwed` enemies in B2 are a smaller job than that number
suggests, and **the tilt-and-redden treatment is engine work that does not exist yet** — it should be
built before the art pass, because it is what makes two images sufficient. It also belongs under tuning:
tilt angle and redness are numbers.

---

### B2. Card and enemy art ◐

**148 of 419 cards** and **13 of 36 enemies** carry `artOwed: true`: written, named, drawing a labelled
placeholder rather than a 404. `!designDocs/honeycomb/tools/generate-placeholder-art.py` builds them and the `.generated.txt`
sidecars keep real art safe from the generator.

**The enemy half of this is re-scoped by B21** — two poses per common enemy, hit states in-engine.

---

### B3. Card art prompts ◐

> Deepseek really deep-dicked everything it touched, nearly all of the suggested prompts it laid out are
> super similar, it'll turn out really boring. I'd like to see how creative you can be, so please narrow
> in on Brienne for now.

Brienne is hand-authored in `../tools/card-prompts.js` `OVERRIDE` and compiles clean through the webui engine.
Nettle, Severine, Cinder, Clemence, Cassadora and neutral are still the table-derived first pass.

**Noodle, 2026-09-25**, in the second demo's needs list (`../BASICS.md`, the second demo):

> - Card art first pass (blocked by picking alt outfits since alt outfits in art is a great way to make them distinct and identify archetypes)

**A second blocker he did not list: the pool itself.** `../rework/cards/CARD-POOL-02.md` cuts the pool
from 192 pool cards (plus 31 Clemence broken forms) to 120 plus 12 neutrals, and renames or rewrites
many of the survivors. Drawing the live pool spends about 70 cards' art on cards that are leaving, so the
order is: his vetoes on the grids, then the alt outfit picks (B26), then this pass, with the prompts
regenerated from the new tables by `../tools/card-prompts.js` and every card's outfit named in its prompt
as he asks.

---

### B24. Anastasia's charactersDB entry and her three alt outfits (session 42) ◐

**She is in `charactersDB.js` now.** `.hcChessV` / `.hcChessC`, codename **`hc-ch3ss`** (the folder name with
one letter swapped for a digit, as the step-2 rule says), registered as a `bodyBrand` singleton, aliased in
`cleaningDB.js` ("Common Mistaggings") and given a `namesArray` block in `aliasDB.js`. Identity and
`*default` were transcribed from the tags constant across **all 114** `chess1V` sidecars, so the entry is
her drawing rather than a reading of it. Her pose-method and pose-damage blocks are **not** written: those
are generated by `refs-shortcuts.js` and she is not in `refsPNG/characters/` yet.

> Downturned eyes is a scene keyword, not body. It also shouldn't be part of her character.

Done. It was in `bodyKeywordDB` `bodyEyes`; it is in `sceneDB` beside `glare` now and out of her entry
entirely. **It was also doing damage**: `defaultDB.js:1651` carries `downturned eyes; glare` with no
exception list, so every image of her — breakdowns included — was getting a `glare` she never asked for.

> Have her take the bare name.

Done. `anastasia` resolves to her, taking it from `anastasia (fate)`. Verified: `anastasia, default`
compiles to her identity plus the chess1V outfit.

> belly overhang is part of anastasia's default outfit, not sure what category it should live in, maybe
> body is fine, but it only works when she's wearing a skirt.

**Category kept as `bodyWaist` (it is a body shape), but the tag moved out of her entry and into the
OUTFIT lines**, which makes the conditional part structural rather than a rule: an outfit with no
waistband simply does not list it. It rides `*default` and the four Cruel designs that wear trousers; the
Radiant gown and the Composed bodysuit correctly go without.

**The three alt outfits are drafted and generated** — `outfit-designs.js` now holds 132 designs, 18 of
them hers (radiant / cruel / composed, A–F each). Built against this folder's own rules: three silhouettes
that share nothing (**carved court gown / headsman's apron over bare arms / asymmetric clockwork
half-frame**), D/E/F as the counterparts of A/B/C, and a different lewd medium in every one of the nine.

> I tested your Anastasia outfits, they're of mixed quality. Celestial isn't very strong.

The first attempt made The Radiant a generic white-and-gold angel. This pass makes Celestial **the white
side of the board** rather than a heaven: alabaster, carved, first-move authority, and in C it drops the
gown for white plate entirely. Awaiting his read on whether that lands.

**No invented tags**, per his note on `OUTFITS.md`. Where a word was missing it was added to the real
dictionaries instead: `scepter`/`sceptre`, `cleaver`, `sickle`, `chain necklace`, `torn bandages`,
`body paint`, `low bun`, `snowflake hair ornament`, `body writing`, `oil`/`oiled`, `hip vent`, and
`scepter`/`cleaver`/`sickle`/`crown` into sceneDB's held-object list so `holding X` resolves. Five tags
remain uncategorised on purpose — `cleaver on hip`, `sickle on hip`, `bone hair ornament`,
`holding chain`, `bandaged arms` — which is the positional/ornament convention this folder already ships
(`dagger on hip`, `mace on back`, `red gem chest ornament`). Engine suite **705 passed, 4 failed**, the
same four as before the session and the same four with her entry removed.

`outfits-generate.js` gained **`--save`**, which asks Forge to keep its own copy in `output\txt2img-images`
so a run can be looked at from a phone. Off by default, since the runner writes the PNG itself.

---

### B26. The whole alt-outfit set, redone (session 42, round 04) ◐

> Some of the outfits came out alright, but, and I don't want to sound rude, it feels like they came out
> alright in spite of your efforts. Now I have stuff like "wax" in the data that probably did nothing, but
> needs cleaning.

> Please redo, I've already saved all my favorites from this attempt line. Decently high priority since I
> can't include the alt outfits in training data until I actually pick them out!

> tan lines, mud, wax, ice, love potion, inflation, hypnosis, and objectification are banned. Downblouse
> and armpit peek are hard to use, be sure you know what you're doing.

**All 114 non-Anastasia designs rewritten.** Her 18 (round 04 already) were kept. `outfit-designs.js` is
132 designs across 22 outfits, and **`invented` is now zero** — the 57 † tags are gone, per his standing
note to move closer to what the model actually recognises.

**What "did nothing" turned out to be, concretely.** Three separate classes, all measurable:

1. **The 57 invented tags.** Gone entirely, not replaced with new ones.
2. **THE COLOURS.** This is the big one and it was invisible. `modifierKeywordDB.colors` holds **44**
   colour words, and the round-03 designs were full of words that are not among them — `cream`, `oat`,
   `sage`, `rust`, `indigo`, `ivory`, `burgundy`, `crimson`, `moss`, `brass`, `rose`. Every one of those
   reached the model as an unknown adjective, so "the recoloured one" came out an arbitrary colour and the
   B variants never actually differed from the A variants the way the brief says they should. All remapped
   onto the real 44 (crimson→maroon, cream→beige, oat→tan, sage→green, rust→orange, indigo/ivory→violet
   and beige, burgundy→maroon, brass→bronze, rose→pink).
3. **THE POSE WAS OVERRIDING THE PROP SWAP.** `PoseA` carries each character's own weapon — Brienne's
   `holding sword, planted sword`, Cinder's `holding spear` — so a B variant that swaps the weapon was
   being handed the original weapon anyway, by the pose, in every single image. **33 designs now carry
   `!holding sword` / `!planted sword` / `!holding spear`**, and Cinder's vanguardPlume keeps her spear
   because there it IS the prop. Verified in a dry run: `marshal-A` renders with a rapier and no spear.

Uncategorised tags went 112 → 55, and what is left is the positional/ornament convention this folder
already ships (`greatsword on back`, `frog on shoulder`, `candle on floor`, `bone hair ornament`).
Words added to the real dictionaries rather than invented: `chainmail`, `sackcloth`, `sideless outfit`,
`stole`, `cane`, `playing card`/`tarot card`, `rapier`, `lance`, `piercing`, and `ash`/`embers`/`spores`/
`petals` as fx. **`latex` was removed again** from `clothesFullwear` — it is already a material modifier in
`modifierKeywordDB.colors`, and one word in two dictionaries in the same space is the bug, not the fix.

**Silhouettes were re-separated per character**, since round 03b's lesson had drifted: Brienne now reads
spiked battle plate / enclosing wall-plate with tassets / cloth alms robes; Cassadora star gown /
pinstripe trouser suit / patchwork bog poncho; Cinder plumed cavalier / military uniform / ember dancer.
Every D/E/F is a counterpart of its A/B/C rather than a fourth idea.

**Generated V-only** with `--extra "action pose, dynamic"`, which is a new option:

> And if you could also add "action pose, dynamic" to the prompts when generating, I'd appreciate it.
> That'd give each outfit more of a chance to win me over than standing stock-straight.

`--extra` appends tags to every prompt in a run; `--save` (added the same session) has Forge keep its own
copy in `output\txt2img-images` so a run can be read from a phone. The C set has not been run.

**The point of the exercise, in his words, so the next pass does not optimise for the wrong thing:**

> The main thing about images is the variety among them creates a natural source of inspiration. Usually
> the ideal outfit isn't any of the images themselves, but something that comes to me when I'm pouring
> over them, hence the desire to get such stark differences and try out new tags.

So spread beats polish here. A design that is merely *good* and looks like its neighbour is worth less
than two that are stark and half-wrong.

**Round 03's data is kept** at `_archive/` as `outfit-designs.round03.bak.js` — he said he had already
saved his favourites from that line, so the images are his, but the tag lists that produced them are worth
keeping until he has picked.

**Round 05, 2026-09-25 — four characters, as combo blocks.** Noodle asked for a fresh A–F set for Cassadora,
Cinder, Clemence and Anastasia, in the paste-in format rather than `outfit-designs.js`:

> I need a full new A-F set for all four of them following the outfit design rules. I'll let them cook
> overnight and pick out the favorites.

> Note that clement is blind and both she and Cinder should always have her eyes obscured by something.
> Cinder's can be her hair, but Clement's must be something that can be destroyed/burn away. It's also
> noting that an upcoming rework will add more fire theming to Clement's broken state attacks.

Written to `outfits-round5/` (four `PROMPTS-*.txt`, one paste each, and `outfits-round5/README.md` with the media table).
All 72 compile headlessly to 18 variants a block. Every tag was checked with `webui2-inspect.js`; two things it
turned up in round 04's data are recorded in that README (`moss` aliases to Nettle's identity, `bare shoulders`
compiles to nothing). Round 04's entries for these four stay in `outfit-designs.js` until he picks.

**Noodle, 2026-09-25**, in the housekeeping message that set the second demo's gate (`../BASICS.md`, the
second demo), the second line of what the demo needs:

> - Give alt outfits actual images

Today every alt outfit is a recoloured copy of the default's pictures (`../rework/starters/` S59-1,
archived), so *"actual images"* means the full picture set per alt: the tiers in `../reference/ART-GUIDE.md`
§1, for 18 alts. The pick comes first: round 05's prompts for Cassadora, Cinder, Clemence and Anastasia are
written and his to choose from; Brienne, Nettle and Severine stand on round 04's `outfit-designs.js`. His
own card pool brief hangs on this too: `../rework/starters/` B1 gates the alts, and the alt's cards are
what the gate protects.

### B27. A drop-in sprite import with a declared size (session 43) ☐ DEFERRED

Raised while sizing Anastasia's chess pieces, where ten of twelve had no scale and every one rendered at
the shared canvas height.

> It seems like we need some kind of better universal solution to deal with #2 for making future work
> easier. Some way I could just drop in enemy sprites as pngs and say "Okay, this enemy's small", then an
> export function would convert them to usable enemy sprites, but I think we need to defer that for now
> to work on Anastasia.

**Explicitly deferred by Noodle — do not build this in an Anastasia session.** Recorded now so the need is
not rediscovered a third time.

The shape it wants, from the problem it is solving: a loose PNG plus a **declared size class** ("small"),
and an export step that produces the game-ready asset already framed for that class — so the size lives
with the drawing instead of being re-derived as a per-entry `presentation.scale` guess every time. Note
the case that broke the single-number approach: a knight that is "tall but thin" and a queen that is
"pawn-sized but floating and her ribbons take up space" are not one scale each, so a size class probably
needs a silhouette, not just a height.

Interim stopgap in use: per-piece `presentation.scale` values, `../chessmaster/FEEDBACK.md` A18.

---

### S47. Wounded sprites are still being drawn for enemies that should not have them ☆

*Filed here session 47 from `../chessmaster/HANDOFF-S47.md`. Noodle's words, byte for byte.*

> Common enemies (and golems) still have wounded sprites. I was quite specific that I wanted common enemies to have just two images: combat, and offense.  Please remove them, any player characters, bosses, and characters manually set to have images for it should still have the wounded state images.

The rule as he states it, unpacked:

| Who | Wounded tier |
|---|---|
| Common enemies, and the twelve chess pieces | **No.** Two images: combat and offense. |
| Player characters | Yes. |
| Bosses | Yes. |
| Anything manually opted in | Yes. |

Where it happens: `honeycomb-art.js`, `spriteChain` picks the `2-` tier by health. The rule wants that
tier skipped unless the subject is a character, a boss, or carries an opt-in — `woundedArt: true` is the
obvious shape for the opt-in, since it makes the exception a field on the entry rather than a list in the
engine. Worth checking what actually EXISTS on disk at the same time: a `2-` file that is never drawn is
dead weight in the build.

---

### B38. Which enemies were missed, and which are still recolours ☆ — FILED FROM THE INBOX, SESSION 51

> He also missed several enemies to work on, and I don't want lazy-looking recolors, so I had him
> bench the scrap salvager.

He did not say which enemies were missed, so this was measured instead of asked. Two things can be read
off the disk: which enemies still stand on a **generated stand-in** rather than their own drawing, and
which of those stand-ins are a **recolour of another enemy in the game**.

**How to tell.** Every folder the placeholder generator wrote carries a `.generated.txt` manifest listing
what it made. An enemy whose `1-combat.webp` is named in that manifest is still wearing a stand-in. An
enemy with no manifest has its own drawing. Twenty-three enemies got real drawings on 2026-09-21 and
have no manifest; twenty-six still have one.

**Eleven enemies still stand on a generated stand-in and were never flagged as owing art.** These are
the missed ones:

| Enemy | Stands on |
|---|---|
| Sporeling | `smol` |
| Cap Brute | `brute` |
| Gloom Wisp | `caster` |
| Hollow Knight | `warrior` |
| Earthstar | `gardener` |
| Bracket Elder | `sage` |
| Spore Alchemist | `alchemist` |
| Bark Sentinel | `shield` |
| Kobold Scavenger | `scavenger` |
| The Matriarch | `matriarch` |
| The Juggernaut | `juggernaut` |

Each of those has a source drawing of its own, so none of them is a recolour of another enemy — they
are simply the older art, untouched by the 2026-09-21 pass. The Juggernaut is the odd one out: it is
already listed in `v13 spire images/_source/enemy inspo variants/assigned/ASSIGNED.md` as getting `treegolem2-c`, which *"Replaces his current art
outright"*, and that has not happened.

**Three enemies ARE literal recolours of another enemy.** `ENEMY_SOURCES` in
`!designDocs/honeycomb/tools/generate-placeholder-art.py` names the source, and a `recipe` entry means the pixels are the
other enemy's, hue-shifted:

| Enemy | Is | Verdict |
|---|---|---|
| **Scrap Salvager** | the **Kobold Scavenger**, recoloured | **His bench was right.** This is the recolour he remembered. |
| Mold Leech | the **Gloom Wisp**, recoloured | Already known: `v13 spire images/_source/enemy inspo variants/assigned/ASSIGNED.md` has no candidate for it and his call was *"cut or reconceptualize"*. |
| Hollow Champion | the **Hollow Knight**, recoloured | Defended on purpose in `v13 spire images/_source/enemy inspo variants/assigned/ASSIGNED.md` — same faction, same armour, they already fight together in three encounters. |

The twelve chess pieces are a fourth case and are Anastasia's, not Act 1's: the six Infernal pieces are
the six Celestial pieces hue-shifted, which `ENEMY_SOURCES` says is deliberate until their own drawings
arrive.

---

### B39. Re-running the placeholder generator would overwrite real art in 26 folders ☆ — FOUND SESSION 51

Not reported by anyone. Found while measuring B38, and worth fixing before it costs a drawing.

`generate-placeholder-art.py` protects real art with the manifest rule, and the manifest file says so in
its own header: *"Replacing a placeholder with real art? Delete its line below and it is never
overwritten."* The rule in code is `if FORCE is False and name in existing and name not in claimed` —
a file **named** in the manifest is overwritten, a file not named in it is kept.

**Twenty-six enemy folders still list `1-combat.webp` and `1-offense.webp` as claimed.** For the eleven
in B38 that is correct — those files really are the generator's. The hazard is that the manifest is the
only thing standing between a rerun and the art, and nothing checks that it agrees with what is on disk.

Two things worth doing, neither of them urgent and neither of them a design question:

- Have the suite assert that no folder claims a file that is demonstrably a hand-drawn asset. There is
  no marker for that today, which is the actual gap.
- Delete the claimed lines as each enemy's real drawing lands, which is what the header asks for and
  what the 2026-09-21 pass did by accident — those twenty-three folders have no manifest at all.

**A related thing that looks like a bug and is not.** The twenty-three newly drawn enemies have
`1-combat.webp` and no `1-offense.webp`. That is deliberate: the generator's own note says *"An enemy
whose `1-combat` is real art gets NO generated files beside it"*, because a generated attack pose cut
from the old stand-in would turn the enemy into a different creature every time it attacked. The game
falls back to `1-combat` for the attack. What is owed is twenty-three real attack drawings, not a
generator run.

**Confirmed by accident, 2026-09-24.** A session ran `generate-placeholder-art.py` by mistake (it has
no `--help`; asking for one runs it) and it rewrote 396 webp files. 369 came out byte-for-byte the
same size, but 27 did not, and they were real work: `map/preview-shop` was flattened from an 85KB
picture to a 2KB placeholder; the twelve `cards/art/*-broken` and `*-broken-tall` pictures fell back to
the plain recipe; and thirteen enemy poses (`capBrute`, `drownedSalvager`, `gardener`, `gloomWisp`,
`hollowChampion`, `hollowKnight`, `matriarch`, `moldLeech`, `sage`, `shield`) were redrawn. All 26 were
put back from the 2026-09-21 snapshot at `D:\syrup-town13 spire images`, each matching the size
recorded minutes before the run. The one that could not be (`characters/priest/ecstatic/recover`) is on
its folder's generator list, so it is a stand-in. `preview-shop` was taken off the panel recipes.
**Until this item is fixed, do not run the generator without first copying `v13 spire images`.**

---

### P27. Enemies turn around while attacking ☆ — TRACED SESSION 55, AND IT IS THE DRAWINGS

> Enemies turn around while attacking sometimes

**It is not the mirror, and it is not the animation. Both were checked and both are innocent.**

1. **The mirror is switched on but nothing opts into it.** `honeycomb.art.spriteFlipped` only flips a
   fighter whose definition carries `sideFacingArt: true` AND who is standing on the side it does not
   belong to. Counted across every enemy and every character: **zero carry `sideFacingArt`.** So the
   flip can never fire, and the `hcMirrored` class is never added.
2. **The acting animation does not flip either.** `.hcActing` is `translateY` plus `scale(1.04)`, and
   `.hcLunge` is a `translateX`. Neither has a `scaleX`, so nothing in the CSS turns a fighter round.

**What is left is the drawing.** An attacking enemy swaps from its `-combat` pose to its `-offense`
pose, and if those two were drawn facing opposite ways, the swap IS the enemy turning around. That
matches the report exactly, including the "sometimes" — it would happen for the enemies whose two files
disagree and for no others.

**How to find which ones.** Open an enemy's `1-combat.webp` and `1-offense.webp` side by side under
`v13 spire images/enemies/<index>/default/`. Fifty-two enemies have the pair, so this wants his eye or a
pass with the art bench rather than a script: no tool can tell which way a drawing faces.

**No file was touched.** The images folder was left alone entirely this session, and a wrongly-faced
`-offense` pose is repainted rather than deleted.

---

## Unsorted — drop new reports for this workstream here

*(a report that does not clearly belong to this workstream goes in `../FEEDBACK.md` instead)*
