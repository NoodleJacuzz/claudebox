# Cloud sessions of 2026-09-24 and 2026-09-25 — import list for the desktop

**For the desktop Claude: read this first, then the folders it names, in the order given.** Two cloud
sessions ran against a snapshot of the design docs that Noodle uploaded to the `claudebox` repository
(branch `claude/honeycomb-quality-lab-wcxd2g`, pull request #1). The desktop repository is the source
of truth; the cloud copy had no sound library, no image folder and no `.claude/` tooling, so nothing
was run in a browser and the suite did not run. Everything below was written from reading the code.

In this repository the design docs live at `designDocs/honeycomb/`; on the desktop the same folder is
`!designDocs/honeycomb/`. Paths below are written the desktop way.

**Status: warm.** Updated as the sessions go; Noodle asks for the final version at the end of Friday.

---

## 1. What was talked about

Two projects, in the order they arose, each with a direct plan of its own. Both follow the rule
Noodle set on 2026-09-25: the first demo is out, the old demo list is stale, and each of the game's
major shortcomings gets a fresh folder with a direct, actionable plan rather than a half-finished item
from an earlier time.

| Project | One line | Folder | State |
|---|---|---|---|
| **Quality Lab** | A bench for tuning how an attack looks and sounds: a fixed cycle on its own screen, a tap recorder, a replay with overrides on, a GO LIVE button into a real fight, exportable timing data, and templates a card-writing agent can pick from. | `!designDocs/honeycomb/quality_lab/` | Designed and revised on his answers. Not built. |
| **Ownerless cards** | A card's owner is assigned when the card is acquired and is never a field on the card's entry; enemies can share pools; the neutral-card Broken-character bug goes away. | `!designDocs/honeycomb/ownerless_cards/` | Plan being written 2026-09-25. Not built. |

The two do not wait on each other. The lab needs three small engine fixes (its `BRIEF.md` P0) that
the ownerless plan also contains; whichever lands first carries them.

---

## 2. Import list

Copy the new folders whole. Merge the edited root files by hand, because the desktop versions may
have moved on since the upload; the exact edits are listed in §3 so they can be re-applied.

| From this repository | To the desktop | Action |
|---|---|---|
| `designDocs/honeycomb/quality_lab/` (BRIEF, CATCH-UP, FEEDBACK, `_archive/`) | `!designDocs/honeycomb/quality_lab/` | copy the folder |
| `designDocs/honeycomb/ownerless_cards/` | `!designDocs/honeycomb/ownerless_cards/` | copy the folder (once it exists in the branch) |
| `designDocs/honeycomb/handoff/` (this file) | `!designDocs/honeycomb/handoff/` | copy the folder |
| `designDocs/honeycomb/CATCH-UP.md` | `!designDocs/honeycomb/CATCH-UP.md` | merge: two workstream rows and the session lines (§3) |
| `designDocs/honeycomb/FEEDBACK.md` | `!designDocs/honeycomb/FEEDBACK.md` | merge: two index rows, an inbox block, a stale-notice (§3) |
| `designDocs/honeycomb/BASICS.md` | `!designDocs/honeycomb/BASICS.md` | merge: one block at the top of "The demo scope" (§3) |
| `designDocs/honeycomb/performance/FEEDBACK.md` | `!designDocs/honeycomb/performance/FEEDBACK.md` | merge: the A8 heading and the block under it (§3) |

No game code changed in either session. Nothing under `scripts/` needs importing.

---

## 3. The root-file edits, so they can be re-applied by hand

- **`CATCH-UP.md`, "Workstreams" table:** a `Quality Lab` row (0 open) and an `Ownerless cards` row,
  both after `Balance tests`. "Recent sessions" table: lines `62 (cloud)`, `62b (cloud)` and
  `62c (cloud)` at the top; copy them from the cloud file.
- **`FEEDBACK.md`, "The index" table:** rows for `quality_lab/` and `ownerless_cards/` after `desk/`,
  with their open counts (run `tools/feedback-audit.js` after import; it must agree). Above
  "The top priority (session 39)": a four-line notice that the top-priority and demo-gate sections are
  out of date since 2026-09-25. In the inbox: a block headed "Ownerless cards (Noodle, 2026-09-25)"
  pointing at the new folder.
- **`BASICS.md`, "The demo scope":** a block headed "Out of date (Noodle, 2026-09-25)" directly under
  the heading, quoting him that the first demo is out and the list has not been rewritten, and telling
  sessions not to rank new work by it. The list itself is unchanged.
- **`performance/FEEDBACK.md`, A8:** heading changed to "Telemetry — SHELVED (Noodle, 2026-09-25)",
  with his sentence under it and a "Do not build this" line; the old text stays below as history.

---

## 4. Decisions Noodle made on the cloud, with where the verbatim words are

| Decision | Where his words are |
|---|---|
| The lab's first build handles sound effects and screen shake; Impact is what each target is measured from | `quality_lab/_archive/FEEDBACK-DONE.md` Q1 |
| The lab is its own simpler screen, transformable into a live battle; the Performance Delay Test must work everywhere | Q2 |
| Player cards must be testable in enemy hands; enemies must be able to use every card | Q3 |
| Owners are assigned in-game and never on a card's entry; an inherently ownerless system | Q4 (both statements) and `ownerless_cards/FEEDBACK.md` |
| A card's sound field and the sound table both stay, as priorities; the holes get fixed | Q5 |
| Templates first, new templates as needed; a per-card override first asks whether the asset or template is wrong; a jumping attack has a start beat and an impact beat | Q6 |
| The first demo is out; the old demo list and its items are from an earlier time; each shortcoming gets a fresh direct plan | Q7, and `BASICS.md` "The demo scope" |
| Telemetry is shelved | Q8, and `performance/FEEDBACK.md` A8 |

---

## 5. After import, on the desktop

1. `node "!designDocs/honeycomb/tools/test-honeycomb.js"` — nothing here changed code, so the suite
   should read as it did before the upload.
2. `node "!designDocs/honeycomb/tools/doc-links.js"` — every pointer in the two new folders resolves
   against a full checkout; report anything it names.
3. `node "!designDocs/honeycomb/tools/feedback-audit.js"` — the two new index rows must agree with
   their folders. (On the cloud copy the audit also reported `map` and `chessmaster` disagreements
   that predate these sessions and were left alone.)
4. Read `quality_lab/BRIEF.md` §2 with the game running: it describes where each animation target
   fires today and was read from the code, not measured. The first thing worth measuring is the claim
   that a party card's sound and hit fire in the same task while an enemy move's sound leads its hit
   by `enemyActionGapMs`.

---

## 6. The goals list this founds

To be completed at the Friday wrap-up. Candidates so far, in Noodle's priority as stated: ownerless
cards (smaller, and it removes a live bug), then the Quality Lab's Phase 0 (the timeline, no UI),
then the lab screen.
