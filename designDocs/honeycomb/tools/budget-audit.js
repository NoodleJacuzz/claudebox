/**
 * Kept so the many documents that name `budget-audit.js` still point at something. It runs
 * BALANCE TEST: BASIC BITE, which took over from it in session 53 (tooling/BALANCE-BRIEF.md Step 3).
 * Every option it had still works, and `--workers N` is new. The tool is `tools/balance/basic-bite.js`.
 */
require("./balance/basic-bite.js");
